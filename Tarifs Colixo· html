<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Calculateur tarifaire Colixo</title>
  <style>
    :root {
      --bg: #070707;
      --panel: rgba(255,255,255,0.07);
      --line: rgba(255,255,255,0.1);
      --text: #ffffff;
      --muted: #cccccc;
      --accent: #ff7a00;
      --accent-2: #ffb347;
      --success: #25c46b;
      --shadow: 0 20px 60px rgba(0,0,0,0.35);
      --radius: 26px;
      --max: 1180px;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: Inter, Arial, sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at top right, rgba(255,122,0,0.16), transparent 24%),
        linear-gradient(180deg, #050505, #111111);
      line-height: 1.5;
    }
    .container {
      width: min(92%, var(--max));
      margin: 0 auto;
      padding: 34px 0 60px;
    }
    .hero {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 22px;
      align-items: center;
      margin-bottom: 28px;
    }
    .hero-card, .panel {
      background: linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.05));
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .hero-card {
      padding: 34px;
    }
    .hero-card h1 {
      font-size: clamp(2rem, 4vw, 4rem);
      line-height: 1;
      letter-spacing: -0.04em;
      margin-bottom: 14px;
      max-width: 12ch;
    }
    .hero-card p {
      color: var(--muted);
      max-width: 60ch;
      font-size: 1.04rem;
    }
    .hero-side {
      padding: 28px;
      display: grid;
      gap: 14px;
    }
    .tag {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      width: fit-content;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(255,255,255,0.07);
      border: 1px solid var(--line);
      font-size: .92rem;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 0.95fr;
      gap: 22px;
    }
    .panel {
      padding: 28px;
    }
    .panel h2 {
      font-size: 1.65rem;
      margin-bottom: 18px;
      letter-spacing: -0.03em;
    }
    .fields {
      display: grid;
      gap: 16px;
    }
    .row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }
    label {
      display: block;
      margin-bottom: 7px;
      font-weight: 700;
      font-size: .92rem;
    }
    input, select {
      width: 100%;
      padding: 15px 16px;
      border-radius: 16px;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(255,255,255,0.05);
      color: #fff;
      font: inherit;
      outline: none;
    }
    input:focus, select:focus {
      border-color: rgba(255,122,0,0.7);
      box-shadow: 0 0 0 4px rgba(255,122,0,0.14);
    }
    .options {
      display: grid;
      gap: 10px;
      margin-top: 4px;
    }
    .check {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 12px 14px;
      border-radius: 16px;
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--line);
    }
    .check input { width: auto; }
    .result {
      display: grid;
      gap: 14px;
    }
    .price-box {
      padding: 24px;
      border-radius: 22px;
      background: linear-gradient(135deg, rgba(255,122,0,0.16), rgba(255,255,255,0.05));
      border: 1px solid var(--line);
    }
    .price-box small {
      color: var(--muted);
      display: block;
      margin-bottom: 8px;
      font-size: .92rem;
    }
    .price {
      font-size: clamp(2.1rem, 5vw, 3.6rem);
      font-weight: 900;
      letter-spacing: -0.05em;
    }
    .breakdown, .mini-card {
      padding: 18px;
      border-radius: 20px;
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--line);
    }
    .breakdown h3, .mini-card h3 {
      margin-bottom: 10px;
      font-size: 1.02rem;
    }
    .line {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      padding: 8px 0;
      color: var(--muted);
      border-bottom: 1px solid rgba(255,255,255,0.06);
    }
    .line:last-child { border-bottom: none; }
    .line strong { color: #fff; }
    .cta {
      margin-top: 14px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 14px 20px;
      border-radius: 999px;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      color: #111;
      font-weight: 800;
      text-decoration: none;
    }
    .note {
      color: var(--muted);
      font-size: .92rem;
    }
    @media (max-width: 980px) {
      .hero, .grid { grid-template-columns: 1fr; }
    }
    @media (max-width: 720px) {
      .row { grid-template-columns: 1fr; }
      .panel, .hero-card, .hero-side { padding: 22px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <section class="hero">
      <div class="hero-card">
        <div class="tag">Calculateur tarifaire Colixo</div>
        <h1>Estimez vos tarifs en quelques secondes</h1>
        <p>Choisissez le poids, le délai et les options souhaitées. Le prix s’ajuste automatiquement pour vous donner une base claire et professionnelle.</p>
      </div>
      <div class="hero-side panel">
        <div class="mini-card">
          <h3>Tarifs intégrés</h3>
          <p class="note">48h, 24h, express, abonnements et suppléments principaux inclus.</p>
        </div>
        <div class="mini-card">
          <h3>Idéal pour Colixo</h3>
          <p class="note">Parfait pour présenter vos prix à un client ou établir rapidement un devis indicatif.</p>
        </div>
      </div>
    </section>

    <section class="grid">
      <div class="panel">
        <h2>Paramètres</h2>
        <div class="fields">
          <div class="row">
            <div>
              <label for="mode">Type de client</label>
              <select id="mode">
                <option value="standard">Tarif standard</option>
                <option value="abonnement">Tarif abonnement</option>
              </select>
            </div>
            <div>
              <label for="service">Délai</label>
              <select id="service">
                <option value="48h">48h</option>
                <option value="24h">24h</option>
                <option value="express">Express 6h</option>
              </select>
            </div>
          </div>

          <div class="row">
            <div>
              <label for="weight">Poids (kg)</label>
              <input id="weight" type="number" min="0" step="0.1" value="5" />
            </div>
            <div>
              <label for="distance">Distance directe (km)</label>
              <input id="distance" type="number" min="0" step="1" value="0" />
            </div>
          </div>

          <div class="options">
            <label>Options</label>
            <label class="check"><input type="checkbox" id="pickup" checked /> Collecte à domicile (+ CHF 5.00)</label>
            <label class="check"><input type="checkbox" id="sameDay" /> Livraison le jour même (+ CHF 10.00)</label>
            <label class="check"><input type="checkbox" id="timeslot" /> Créneau horaire précis (+ CHF 5.00)</label>
            <label class="check"><input type="checkbox" id="remote" /> Zone éloignée (+ CHF 8.00)</label>
            <label class="check"><input type="checkbox" id="waiting" /> Attente &gt; 10 min (+ CHF 10.00)</label>
            <label class="check"><input type="checkbox" id="direct" /> Transport direct au km (CHF 2.00 / km)</label>
          </div>
        </div>
      </div>

      <div class="panel result">
        <h2>Résultat</h2>
        <div class="price-box">
          <small>Prix estimatif</small>
          <div class="price" id="price">CHF 0.00</div>
        </div>

        <div class="breakdown">
          <h3>Détail du calcul</h3>
          <div class="line"><span>Catégorie</span><strong id="category">—</strong></div>
          <div class="line"><span>Tarif de base</span><strong id="basePrice">CHF 0.00</strong></div>
          <div class="line"><span>Suppléments</span><strong id="extras">CHF 0.00</strong></div>
          <div class="line"><span>Total</span><strong id="total">CHF 0.00</strong></div>
        </div>

        <div class="mini-card">
          <h3>Message client</h3>
          <p class="note" id="clientText">Tarif indicatif calculé selon les paramètres choisis. Un devis personnalisé peut être ajusté selon le volume et la fréquence.</p>
        </div>

        <a class="cta" href="mailto:info@colixo.ch?subject=Demande%20de%20devis%20Colixo">Demander un devis</a>
      </div>
    </section>
  </div>

  <script>
    const prices = {
      standard: {
        light: { '48h': 8.9, '24h': 10.9, express: 18.0 },
        medium: { '48h': 11.9, '24h': 14.9, express: 22.0 },
        heavy: { '48h': 18.9, '24h': 22.9, express: 32.0 },
        bulky: { '48h': 29.0, '24h': 34.0, express: 49.0 }
      },
      abonnement: {
        light: { '48h': 7.5, '24h': 9.5, express: 15.0 },
        medium: { '48h': 9.9, '24h': 12.9, express: 19.0 },
        heavy: { '48h': 16.9, '24h': 20.9, express: 28.0 },
        bulky: { '48h': 29.0, '24h': 34.0, express: 49.0 }
      }
    };

    const weightInput = document.getElementById('weight');
    const serviceInput = document.getElementById('service');
    const modeInput = document.getElementById('mode');
    const distanceInput = document.getElementById('distance');
    const optionIds = ['pickup', 'sameDay', 'timeslot', 'remote', 'waiting', 'direct'];

    function getCategory(weight) {
      if (weight < 2) return ['light', 'Colis léger < 2 kg'];
      if (weight <= 10) return ['medium', 'Colis moyen 2 – 10 kg'];
      if (weight <= 30) return ['heavy', 'Colis lourd 10 – 30 kg'];
      return ['bulky', 'Encombrant > 30 kg'];
    }

    function formatCHF(value) {
      return `CHF ${value.toFixed(2)}`;
    }

    function calculate() {
      const weight = parseFloat(weightInput.value || '0');
      const mode = modeInput.value;
      const service = serviceInput.value;
      const distance = parseFloat(distanceInput.value || '0');
      const [categoryKey, categoryLabel] = getCategory(weight);

      let base = prices[mode][categoryKey][service];
      let extras = 0;

      if (document.getElementById('pickup').checked) extras += 5;
      if (document.getElementById('sameDay').checked) extras += 10;
      if (document.getElementById('timeslot').checked) extras += 5;
      if (document.getElementById('remote').checked) extras += 8;
      if (document.getElementById('waiting').checked) extras += 10;
      if (document.getElementById('direct').checked && distance > 0) extras += distance * 2;

      const total = base + extras;

      document.getElementById('category').textContent = categoryLabel;
      document.getElementById('basePrice').textContent = formatCHF(base);
      document.getElementById('extras').textContent = formatCHF(extras);
      document.getElementById('total').textContent = formatCHF(total);
      document.getElementById('price').textContent = formatCHF(total);

      const clientMode = mode === 'abonnement' ? 'tarif abonnement' : 'tarif standard';
      document.getElementById('clientText').textContent = `Tarif indicatif calculé sur la base d’un ${clientMode}, catégorie ${categoryLabel.toLowerCase()} et service ${service}. Un devis final peut être ajusté selon le volume, la fréquence et la zone.`;
    }

    [weightInput, serviceInput, modeInput, distanceInput, ...optionIds.map(id => document.getElementById(id))].forEach(el => {
      el.addEventListener('input', calculate);
      el.addEventListener('change', calculate);
    });

    calculate();
  </script>
</body>
</html>

