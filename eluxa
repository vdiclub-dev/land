
from pathlib import Path
from textwrap import dedent

html = r"""<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Atelier de Pendulerie Eluxa | Présentation Colixo</title>
  <meta name="description" content="Page de présentation premium pour l’Atelier de Pendulerie Eluxa à Yverdon-les-Bains." />
  <style>
    :root {
      --bg: #0e0c08;
      --bg-soft: #17130d;
      --gold: #c89b4a;
      --gold-light: #f4d79b;
      --cream: #fff8ec;
      --muted: #c9bda8;
      --line: rgba(244, 215, 155, .22);
      --white: #ffffff;
      --shadow: 0 24px 80px rgba(0,0,0,.45);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      color: var(--cream);
      background:
        radial-gradient(circle at 20% 10%, rgba(200,155,74,.22), transparent 32%),
        radial-gradient(circle at 90% 20%, rgba(244,215,155,.12), transparent 28%),
        linear-gradient(135deg, #060504 0%, #15100a 50%, #080706 100%);
      min-height: 100vh;
    }

    a { color: inherit; text-decoration: none; }

    .page {
      width: min(1180px, calc(100% - 34px));
      margin: 0 auto;
    }

    .nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 26px 0;
      border-bottom: 1px solid var(--line);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 14px;
      letter-spacing: .08em;
      text-transform: uppercase;
      font-size: 13px;
      color: var(--gold-light);
    }

    .brand-mark {
      width: 42px;
      height: 42px;
      border: 1px solid var(--gold);
      border-radius: 50%;
      display: grid;
      place-items: center;
      color: var(--gold-light);
      background: rgba(255,255,255,.04);
      box-shadow: 0 0 35px rgba(200,155,74,.18);
    }

    .nav-link {
      padding: 12px 18px;
      border: 1px solid var(--line);
      border-radius: 999px;
      color: var(--gold-light);
      background: rgba(255,255,255,.04);
      transition: .25s;
    }

    .nav-link:hover {
      transform: translateY(-1px);
      background: rgba(244,215,155,.12);
      border-color: rgba(244,215,155,.45);
    }

    .hero {
      display: grid;
      grid-template-columns: 1.08fr .92fr;
      gap: 46px;
      align-items: center;
      padding: 72px 0 56px;
    }

    .badge {
      display: inline-flex;
      gap: 10px;
      align-items: center;
      padding: 9px 14px;
      border: 1px solid var(--line);
      border-radius: 999px;
      color: var(--gold-light);
      background: rgba(255,255,255,.045);
      font-size: 13px;
      margin-bottom: 22px;
    }

    h1 {
      margin: 0;
      font-size: clamp(42px, 6vw, 82px);
      line-height: .95;
      letter-spacing: -0.055em;
      max-width: 760px;
    }

    .highlight {
      color: var(--gold-light);
      text-shadow: 0 0 40px rgba(200,155,74,.22);
    }

    .lead {
      margin: 26px 0 0;
      max-width: 650px;
      color: var(--muted);
      font-size: 19px;
      line-height: 1.65;
    }

    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
      margin-top: 34px;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 52px;
      padding: 0 24px;
      border-radius: 16px;
      font-weight: 700;
      transition: .25s;
    }

    .btn-primary {
      color: #171009;
      background: linear-gradient(135deg, var(--gold-light), var(--gold));
      box-shadow: 0 18px 46px rgba(200,155,74,.25);
    }

    .btn-secondary {
      border: 1px solid var(--line);
      color: var(--gold-light);
      background: rgba(255,255,255,.04);
    }

    .btn:hover { transform: translateY(-2px); }

    .visual-card {
      position: relative;
      min-height: 520px;
      border: 1px solid var(--line);
      border-radius: 34px;
      overflow: hidden;
      background:
        radial-gradient(circle at center, rgba(244,215,155,.12), transparent 35%),
        linear-gradient(145deg, rgba(255,255,255,.10), rgba(255,255,255,.025));
      box-shadow: var(--shadow);
    }

    .clock {
      position: absolute;
      inset: 58px;
      border: 1px solid rgba(244,215,155,.28);
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: radial-gradient(circle, rgba(255,255,255,.05), transparent 62%);
    }

    .clock::before {
      content: "";
      width: 10px;
      height: 10px;
      background: var(--gold-light);
      border-radius: 50%;
      box-shadow: 0 0 28px rgba(244,215,155,.7);
      z-index: 3;
    }

    .hand-long,
    .hand-short {
      position: absolute;
      left: 50%;
      top: 50%;
      transform-origin: left center;
      background: linear-gradient(90deg, var(--gold-light), var(--gold));
      border-radius: 999px;
    }

    .hand-long {
      width: 150px;
      height: 3px;
      transform: rotate(-36deg);
    }

    .hand-short {
      width: 96px;
      height: 5px;
      transform: rotate(250deg);
    }

    .roman {
      position: absolute;
      color: rgba(244,215,155,.82);
      font-family: Georgia, serif;
      font-size: 26px;
      letter-spacing: .05em;
    }
    .r12 { top: 34px; left: 50%; transform: translateX(-50%); }
    .r3 { right: 42px; top: 50%; transform: translateY(-50%); }
    .r6 { bottom: 34px; left: 50%; transform: translateX(-50%); }
    .r9 { left: 42px; top: 50%; transform: translateY(-50%); }

    .glass-note {
      position: absolute;
      left: 28px;
      right: 28px;
      bottom: 28px;
      padding: 22px;
      border: 1px solid rgba(244,215,155,.18);
      border-radius: 24px;
      background: rgba(10,8,5,.68);
      backdrop-filter: blur(10px);
    }

    .glass-note strong {
      display: block;
      color: var(--gold-light);
      font-size: 18px;
      margin-bottom: 8px;
    }

    .glass-note span {
      color: var(--muted);
      line-height: 1.55;
    }

    .section {
      padding: 54px 0;
    }

    .section-title {
      display: flex;
      align-items: end;
      justify-content: space-between;
      gap: 30px;
      margin-bottom: 26px;
    }

    h2 {
      font-size: clamp(30px, 4vw, 48px);
      letter-spacing: -0.04em;
      margin: 0;
    }

    .section-title p {
      margin: 0;
      max-width: 430px;
      color: var(--muted);
      line-height: 1.6;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
    }

    .card {
      min-height: 214px;
      padding: 24px;
      border: 1px solid var(--line);
      border-radius: 24px;
      background: linear-gradient(145deg, rgba(255,255,255,.075), rgba(255,255,255,.025));
      box-shadow: 0 16px 44px rgba(0,0,0,.22);
    }

    .icon {
      width: 42px;
      height: 42px;
      border-radius: 14px;
      display: grid;
      place-items: center;
      margin-bottom: 20px;
      background: rgba(244,215,155,.12);
      color: var(--gold-light);
      font-size: 20px;
    }

    .card h3 {
      margin: 0 0 10px;
      font-size: 20px;
      color: var(--gold-light);
    }

    .card p {
      margin: 0;
      color: var(--muted);
      line-height: 1.58;
      font-size: 15px;
    }

    .quote {
      margin-top: 20px;
      padding: 40px;
      border-radius: 30px;
      border: 1px solid var(--line);
      background:
        linear-gradient(90deg, rgba(200,155,74,.16), rgba(255,255,255,.04)),
        rgba(255,255,255,.03);
    }

    .quote p {
      font-family: Georgia, serif;
      color: var(--cream);
      font-size: clamp(24px, 3vw, 39px);
      line-height: 1.22;
      margin: 0;
    }

    .quote span {
      display: block;
      color: var(--gold-light);
      margin-top: 18px;
    }

    .info {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 18px;
    }

    .info-box {
      padding: 28px;
      border: 1px solid var(--line);
      border-radius: 26px;
      background: rgba(255,255,255,.04);
    }

    .info-box h3 {
      margin: 0 0 16px;
      color: var(--gold-light);
      font-size: 22px;
    }

    .info-box p {
      margin: 8px 0;
      color: var(--muted);
      line-height: 1.55;
    }

    .final {
      text-align: center;
      padding: 78px 0 90px;
    }

    .final h2 {
      max-width: 820px;
      margin: 0 auto 18px;
    }

    .final p {
      color: var(--muted);
      max-width: 660px;
      margin: 0 auto 30px;
      line-height: 1.7;
      font-size: 18px;
    }

    @media (max-width: 900px) {
      .hero { grid-template-columns: 1fr; padding-top: 44px; }
      .visual-card { min-height: 440px; }
      .cards { grid-template-columns: 1fr 1fr; }
      .info { grid-template-columns: 1fr; }
      .section-title { display: block; }
      .section-title p { margin-top: 14px; }
    }

    @media (max-width: 560px) {
      .nav { align-items: flex-start; gap: 18px; flex-direction: column; }
      .cards { grid-template-columns: 1fr; }
      .clock { inset: 40px; }
      .hand-long { width: 112px; }
      .hand-short { width: 74px; }
      .quote { padding: 26px; }
    }
  </style>
</head>
<body>
  <main class="page">
    <header class="nav">
      <div class="brand">
        <div class="brand-mark">E</div>
        <div>Atelier de Pendulerie Eluxa</div>
      </div>
      <a class="nav-link" href="https://www.atelier-eluxa.ch/" target="_blank" rel="noopener">Site officiel</a>
    </header>

    <section class="hero">
      <div>
        <div class="badge">Yverdon-les-Bains • Pendules anciennes • Savoir-faire horloger</div>
        <h1>L’art de redonner vie au <span class="highlight">temps</span>.</h1>
        <p class="lead">
          L’Atelier de Pendulerie Eluxa accompagne les propriétaires de pendules anciennes
          pour la révision des mouvements, la restauration des cabinets, les conseils,
          les estimations et les interventions liées aux horloges de caractère.
        </p>
        <div class="actions">
          <a class="btn btn-primary" href="https://www.atelier-eluxa.ch/" target="_blank" rel="noopener">Découvrir l’atelier</a>
          <a class="btn btn-secondary" href="tel:+41244261184">Appeler l’atelier</a>
        </div>
      </div>

      <div class="visual-card" aria-label="Illustration horlogère">
        <div class="clock">
          <div class="roman r12">XII</div>
          <div class="roman r3">III</div>
          <div class="roman r6">VI</div>
          <div class="roman r9">IX</div>
          <div class="hand-long"></div>
          <div class="hand-short"></div>
        </div>
        <div class="glass-note">
          <strong>Un atelier dédié aux pendules anciennes</strong>
          <span>Un univers artisanal où précision, patience et respect des traditions se rencontrent.</span>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section-title">
        <h2>Prestations principales</h2>
        <p>Une présentation claire des services proposés par l’atelier pour valoriser son expertise et rassurer les clients.</p>
      </div>

      <div class="cards">
        <article class="card">
          <div class="icon">⚙</div>
          <h3>Révision des mouvements</h3>
          <p>Entretien et révision des mécanismes avec une garantie pouvant aller jusqu’à 5 ans.</p>
        </article>
        <article class="card">
          <div class="icon">⌂</div>
          <h3>Restauration des cabinets</h3>
          <p>Restauration des boîtiers dans le respect des traditions et des techniques artisanales.</p>
        </article>
        <article class="card">
          <div class="icon">✉</div>
          <h3>Envoi par la poste</h3>
          <p>Une solution simple pour faire parvenir une horloge à l’atelier de manière organisée.</p>
        </article>
        <article class="card">
          <div class="icon">⌁</div>
          <h3>Service à domicile</h3>
          <p>Des interventions et conseils adaptés lorsque le déplacement de la pendule n’est pas idéal.</p>
        </article>
      </div>

      <div class="quote">
        <p>« Là où résonnent les carillons, chaque pendule retrouve son histoire. »</p>
        <span>Atelier de Pendulerie Eluxa</span>
      </div>
    </section>

    <section class="section">
      <div class="info">
        <div class="info-box">
          <h3>Contact</h3>
          <p><strong>Adresse :</strong> Rue de la Maison Rouge 23, 1400 Yverdon-les-Bains</p>
          <p><strong>Téléphone :</strong> +41 24 426 11 84</p>
          <p><strong>E-mail :</strong> info@atelier-eluxa.ch</p>
        </div>
        <div class="info-box">
          <h3>Horaires</h3>
          <p><strong>Lundi à vendredi :</strong> 08:00 – 12:00 / 14:00 – 18:00</p>
          <p><strong>Samedi et dimanche :</strong> fermé</p>
          <p><strong>Site :</strong> atelier-eluxa.ch</p>
        </div>
      </div>
    </section>

    <section class="final">
      <h2>Une page élégante pour mettre en valeur un métier d’exception.</h2>
      <p>
        Cette page peut être utilisée comme présentation web, page partenaire, support commercial
        ou base pour une landing page plus complète.
      </p>
      <a class="btn btn-primary" href="https://www.atelier-eluxa.ch/" target="_blank" rel="noopener">Visiter le site officiel</a>
    </section>
  </main>
</body>
</html>
"""

out = Path("/mnt/data/presentation_atelier_eluxa.html")
out.write_text(html, encoding="utf-8")
out.as_posix()
